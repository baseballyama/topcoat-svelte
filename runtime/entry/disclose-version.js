// Entry served for the bare `svelte/internal/disclose-version` specifier.
// Compiled components import it for its side effect (registering the Svelte
// version once per page).
import "svelte/internal/disclose-version";
